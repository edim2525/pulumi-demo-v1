*.pyc
venv/

# Pulumi local state (already stored in ~/.pulumi/)
.pulumi/
# Note: Stack configs in environments/ are tracked in git
# Symlinks at root (Pulumi.dev.yaml, Pulumi.prod.yaml) are also tracked
