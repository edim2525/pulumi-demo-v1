*.pyc
venv/

# Pulumi local state (already stored in ~/.pulumi/)
# We keep a copy in stack-state-dev.json for backup/portability
# Pulumi stack configs (kept in environments/ subdirectories)
pulumi_resources/Pulumi.*.yaml
